[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Set these in Netlify UI for secure storage or via CLI during deploy
  # VITE_SUPABASE_URL = ""
  # VITE_SUPABASE_ANON_KEY = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
